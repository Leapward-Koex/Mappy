# Copy this file to .env.local for local builds (including VS Code).
# Never commit live keys.
# App-restricted Maps SDK for Android key, embedded in every Android variant.
MAPPY_ANDROID_SDK_API_KEY=
# Separate provider credential, embedded only in debug/profile builds.
MAPPY_DEV_GOOGLE_API_KEY=
